# 合域 · 更新日志

记录面向用户与运维的**已发布变更**。每条按时间倒序（最新在上）。

格式说明：

| 字段 | 含义 |
|------|------|
| **日期** | 功能上线或推送到 `main` 的日期 |
| **摘要** | 一句话说明改了什么 |
| **用户可见** | 界面或操作流程上的变化 |
| **接口 / 后端** | API、Worker、存储层变更（如有） |
| **提交 / 部署** | Git 提交与线上版本（便于追溯） |

---

## 2026-07-15（三）· 资料包支持嵌套子文件夹

**摘要**：资料包按路径树展示多层文件夹；可拖入任意子夹；移动弹窗可在已有夹下新建子夹。路径仍写在 `documents.filename`。

### 用户可见

- 上传含相对路径的文件夹后，按「父夹 → 子夹」树形折叠展示（不再把 `尽调/合同` 当成单一平铺组名）。
- 拖拽可落到任意层级文件夹；删除某夹会连同其下全部子夹与文件。
- 移动弹窗「新建」可选父文件夹 + 子夹名（可多层，如 `合同/修订稿`）。

### 接口 / 后端

- 无 Worker 变更；嵌套依赖既有 `folder` 路径与 `/` 分段。

### 提交 / 部署

| 组件 | 版本 |
|------|------|
| Cloudflare Worker | 无变更 |
| GitHub Pages | push `main` 后由 Actions 自动部署 |
| Git `main`（platform） | （本提交） |

---

## 2026-07-15（三）· 资料包：拖拽进文件夹 + 自定义移动弹窗

**摘要**：文件可拖到文件夹标题（或根目录落点）；移动改用站内弹窗，不再用浏览器默认 `prompt`。仍改写 D1 `documents.filename` 逻辑路径。

### 用户可见

- **拖拽**：按住文件拖到目标文件夹标题上松开即可归入；拖到「根目录」区可移出文件夹。
- **弹窗**：点移动图标打开自绘对话框，可选已有文件夹或新建名称（不再是系统灰弹窗）。
- 移动后列表按新路径重新分组。

### 接口 / 后端

- `PATCH /api/projects/:id/files/:docId` `{ userId, folder }`；只更新 filename，R2 key 不变（Worker 已上线，本次仅前端交互）。

### 提交 / 部署

| 组件 | 版本 |
|------|------|
| Cloudflare Worker | `2e38df36-22a7-4b1c-96e2-3c05cfed48f0`（无变更） |
| GitHub Pages | push `main` 后由 Actions 自动部署 |
| Git `main`（platform） | `6959939` |

---

## 2026-07-15（三）· 项目可见性：全开放 / 仅限邀请

**摘要**：创建项目去掉「半开放」；默认「仅限邀请」——未选成员时仅创建人可见；全开放仍对全平台可见。

### 用户可见

- 新建项目开放程度仅两项：**仅限邀请**（默认）、**全开放**。
- 仅限邀请且未邀请任何人 → 总览里只有创建人能看到该项目。
- 邀请谁，谁才能看到；平台管理员仍可见全部。

### 接口 / 后端

- `projects.visibility`：`public` | `invite`（存量默认 `public`）。
- `GET /api/projects?userId=` 按可见性过滤；创建写入 `visibility` 并播成员席位。

### 提交 / 部署

| 组件 | 版本 |
|------|------|
| Cloudflare Worker | `d537a4a6-af05-4e28-90c6-44bb12a01478` |
| D1 迁移 | `0015_project_visibility.sql`（已执行） |
| GitHub Pages | push `main` 后由 Actions 自动部署 |
| Git `main`（platform） | `e264d1a` |

---

## 2026-07-15（三）· 新建项目 / 资料包支持上传文件夹

**摘要**：创建项目与项目资料包可一次选择整个文件夹；列表按路径分组折叠展示（浏览器无法把「文件夹」当单个对象入库，文件仍各自存储）。

### 用户可见

- 新建项目「参考附件」：**选择文件** + **选择文件夹**。
- 项目详情「项目资料与附件」：**上传文件** + **上传文件夹**；文件夹内文件按目录折叠成一组，可整夹删除。
- 入库文件名保留相对路径（如 `尽调/合同/nda.pdf`），列表内只显示文件名，标题为完整路径。

### 接口 / 后端

- 仍逐文件 `POST /api/projects/:id/files`；上传时用相对路径作为 `filename`（无需改 Worker）。

### 提交 / 部署

| 组件 | 版本 |
|------|------|
| GitHub Pages | push `main` 后由 Actions 自动部署 |
| Git `main`（platform） | `2f40536` |

---

## 2026-07-14（二）· Hermes 知识网络「阅读与思考入口」升级 + 收口补丁

**摘要**：保留 Fragment 装配壳，换掉「固定短卡片 + 固定小抄」；短回答模型升为 Qwen3.7-Plus；随后修完 B/C 假全文、未安装 Skill 路由、maturity 旧口径与 embedding/遥测缺口。

### 用户可见

- 普通对话与流式聊天底层模型改为 **Qwen3.7-Plus**（`HERMES_MODEL=qwen3.7-plus`）。
- 全量知识网络生成：按板块检索多段证据；需要时动态加载**已安装**完整 Skill（未安装回退 deep-ref）；附录 B/C 在同 Job 内基于 **全部 13 板块**压缩正文收尾；进度条可显示「全文收尾附录 B/C」。
- 成熟度 Factor A/B 由 Worker 定分写入 masthead（Hermes 不再自评）。
- Job 完成答复可带公开检索遥测与 B/C stub 审计（不再静默兜底）。

### 接口 / 后端

- 统一政策：`knowledge-network-kn-policy.ts`（公开检索 / maturity / deep-ref）。
- 证据预处理：按 Slot 多 chunk（向量优先）；**13 个查询向量一次批量 embedding**。
- 动态 Workflow 路由：映射表静态打包进 Worker；`compliance-check` 等未安装项只读 deep-ref。
- Appendix wrap-up：全量 slot 压缩语料；`appendixWrapupFellBackToStub` 审计。
- 公开检索遥测：`allowed / routingRequested / attempted / succeeded / sourcesAdded`。
- Codex→Hermes 映射 + 加深漂移单测；Hermes Skill/schema 删除 maturity 自评旧口径。
- 浏览器上传知识网络仍宽松覆盖（缺 `schema-version: 2.91` 可写带 warning）。

### 提交 / 部署

| 组件 | 版本 |
|------|------|
| Cloudflare Worker | `8c67ef8d-9668-4527-9dab-2d60079cb96a` |
| Git `main`（platform） | `597eccc`（已 push） |
| Railway Skills | opportunistic-investments-hermes 已按 `597eccc` 同步（GitHub raw → `/opt/data/skills`） |

---

## 2026-07-13（一）· 管理后台真实编辑权限

**摘要**：账号与权限页「编辑权限」可保存各项目角色；权限标签与主平台一致（Advanced 进阶级等）。

### 用户可见

- **账号与权限 → 用户详情 → 编辑权限**：按项目下拉设置 Guest / Basic 基础级 / Advanced 进阶级 / Core 核心级；保存后列表即时刷新。
- 项目**创建人**固定 Core 核心级；**平台 Admin**（如 Candice）只读，不可在此修改。
- 用户详情「可见项目」标签改为完整档位名称（不再显示 Mid / Low 等缩写）。

### 接口 / 后端

- 新增 `GET /api/admin/users/:userId/permissions`：列出用户在各项目的有效角色与是否可编辑。
- 新增 `PUT /api/admin/users/:userId/permissions`：批量写入 `project_member_roles`（与主平台项目权限管理同源）。
- bootstrap 用户 `projectAccess.accessLabel` 统一为完整档位文案。

### 提交 / 部署

| 组件 | 版本 |
|------|------|
| Cloudflare Worker | `d21ce905` |
| Git `main`（admin-portal） | `7aaef0f` |
| Git `main`（platform） | `4c70d94` |

---

## 2026-07-10（六）· 知识库 + Agent 管理对齐真实数据

**摘要**：知识库按项目分组展示真实 RAG 语料与 KN 状态；Agent 管理改为只读技能说明书与深度任务监控。

### 用户可见

- **知识库管理**：按在管项目 → 资料包/对话附件浏览；展示 chunk/向量状态、嵌入模型；底部列出已发布项目知识网络。
- **Agent 管理 → 技能与路由**：只读技能目录（与 Worker chat-modes 同源）、权限矩阵、近期 agent_jobs。
- **运行架构**：真实「意图识别 → 快答 / Hermes」流程图（非 Multi-Agent 演示）。
- **测试沙盒**：标注「即将推出」。

### 接口 / 后端

`GET /api/admin/bootstrap` 新增 `knowledgeCatalog`、`agentsCatalog`。

### 提交 / 部署

| 组件 | 版本 |
|------|------|
| Cloudflare Worker | `dda6c50d` |

---

## 2026-07-10（五）· Token 精确计量

**摘要**：流式聊天、Hermes Agent 任务接入 API 级 Token 计量；统计逻辑合并历史估算与新精确记录。

### 用户可见

- **Token 用量**：新产生的对话与深度任务逐步显示为 API 精确计量；历史仍保留字数估算。
- 页面提示区分「全估算 / 历史估算 + 新对话精确」两种状态。

### 接口 / 后端

- 流式 SSE 解析 `usage` 字段写入 `token_usage_log`（`is_estimated=0`）。
- `completeAgentJob` 收尾时记录 Agent/Hermes 任务 Token（优先解析 Run 原始 `usage`）。
- `buildAdminTokenUsageStats`：同日同会话有计量则跳过消息估算，避免重复计数。

### 提交 / 部署

| 组件 | 版本 |
|------|------|
| Cloudflare Worker | `4afc0943` |

---

## 2026-07-10（四）· 概览活跃趋势 + 在管项目真实指标

**摘要**：概览页活跃用户曲线、在管项目列表的文档/对话/参与人/风险列改为云端实时统计。

### 用户可见

- **概览 → 近 14 天活跃与对话趋势**：按真实对话 `lastActiveAt` / `startedAt` 聚合。
- **在管项目列表**：列改为 **文档数 / 对话数 / 参与人 / 风险程度**（风险由对话合规规则推断）。

### 接口 / 后端

`GET /api/admin/bootstrap` 新增 `activeUserDaily`、`projectStats`。

### 提交 / 部署

| 组件 | 版本 |
|------|------|
| Cloudflare Worker | `f74dbdab` |

---

## 2026-07-10（三）· Agent 认知摘要 + Token 真实统计

**摘要**：在管项目支持 AI 生成并缓存「Agent 认知摘要」；管理后台 Token 用量页与概览 Token 图表改为云端真实/估算数据。

### 用户可见

- **在管项目 → Agent 认知摘要**：首次点击「生成认知摘要」或由 AI 结合项目资料与近期对话生成管理视角摘要；支持「重新生成」；已生成内容会缓存展示。
- **Token 用量**：总消耗、成本、日均、账号明细、组织维度汇总均来自 `bootstrap.tokenUsage`（近 90 天曲线）。
- **概览 → Token**：本月 Token 与近 30 天趋势图接 live 数据。
- 若尚无精确计量记录，页面会标注「基于对话字数的估算」。

### 接口 / 后端

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/admin/projects/:id/cognition` | 读取缓存的认知摘要 |
| `POST` | `/api/admin/projects/:id/cognition` | 调用 LLM 生成并写入缓存 |

`GET /api/admin/bootstrap` 响应新增：

| 字段 | 说明 |
|------|------|
| `tokenUsage` | 近 90 天 Token 统计（`daily` / `byUser` / `byRoleGroup`） |
| `projectCognition` | 各项目已缓存的认知摘要 |

**D1 迁移 `0015`**：`token_usage_log`（对话 LLM 调用计量）、`project_admin_cognition`（认知摘要缓存）。

聊天路径在同步/流式回复后写入 `token_usage_log`；无计量记录时从 `user_chat_messages` 按字数估算。

### 提交 / 部署

| 组件 | 版本 |
|------|------|
| Cloudflare Worker | `d4cd4965` |
| D1 迁移 | `0015_token_usage_and_cognition.sql` ✅ |

---

## 2026-07-10（二）· 管理后台数据真实性增强

**摘要**：在管项目文档、对话审计记录、概览统计数字改为云端实时数据；去掉项目详情底部「（示意）」文案。

### 用户可见

- **在管项目 → 相关文档**：展示 D1 `documents` 表实际上传文件（资料包 + 对话附件），解析状态由向量分块数判断。
- **对话监控 → 会话详情**：新增「审计记录」区块，数据来自 `chat_message_audit_log`（消息创建/删除）。
- **概览**：在管项目数、项目阶段分布、账号数、今日活跃/对话、项目文档数、待合规复核数均来自实时统计。
- **在管项目详情**：移除底部黄色「（示意）」提示。

### 接口 / 后端

`GET /api/admin/bootstrap` 响应新增：

| 字段 | 说明 |
|------|------|
| `projectDocuments` | 各项目文档列表（按 `projectId` 索引） |
| `auditByConversation` | 各会话审计事件（键：`userId::conversationId`） |
| `overview` | 概览页汇总统计 |
| `counts.documents` / `counts.auditEvents` | 文档与审计事件计数 |

新增 Worker 模块：`admin-portal-documents.ts`；`chat-audit.ts` 增加 `listRecentChatAuditLog()`。

### 提交 / 部署

| 组件 | 版本 |
|------|------|
| Cloudflare Worker | `d77f98a8` |
| Git `main`（platform） | `7660f3d` |
| Git `main`（admin-portal） | `5c60875` |

---

## 2026-07-10 · 管理后台联通云端（登录 + 实时数据）

**摘要**：独立管理后台（`family-office-admin-portal`）接入 `jfo-api` Worker，支持管理员登录，并实时展示在管项目、账号权限与对话监控；工作台 `/app/admin` 自动跳转管理后台。

### 用户可见

- **管理后台**（`https://fangjg16.github.io/family-office-admin-portal/`）：
  - 新增登录页，默认账号 **admin**，密码 **admin2026**。
  - **在管项目**、**账号与权限**、**对话监控** 三个页面改为从云端拉取真实数据（不再依赖本地静态快照）。
  - 顶栏显示连接状态，可点击「刷新」重新同步。
- **用户工作台**（`https://fangjg16.github.io/family-office-platform/`）：
  - 平台管理员（`candice-guo`）进入 **管理中枢**（`/app/admin`）时，自动跳转到独立管理后台。

### 接口 / 后端

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/admin/login` | 管理后台登录，返回 Bearer token |
| `GET` | `/api/admin/bootstrap` | 一次性返回项目、用户、对话（需 Bearer） |
| `GET` | `/api/admin/projects` | 项目列表（需 Bearer） |
| `GET` | `/api/admin/users` | 账号与项目权限（需 Bearer） |
| `GET` | `/api/admin/conversations` | 对话监控聚合（需 Bearer） |

**鉴权**：登录成功后前端在 `Authorization: Bearer <token>` 中携带会话令牌；运维脚本仍可使用 `JFO_INTERNAL_KEY`。

**环境变量（Worker）**：

| 变量 | 默认 | 说明 |
|------|------|------|
| `ADMIN_PORTAL_USERNAME` | `admin` | 管理后台登录账号 |
| `ADMIN_PORTAL_PASSWORD` | `admin2026` | 管理后台登录密码（生产建议 `wrangler secret put`） |

本地开发 CORS 已放行 `localhost:5174`（管理后台 dev 端口）。

### 提交 / 部署

| 组件 | 说明 |
|------|------|
| Git `main`（platform） | `1cd067d` — *Connect admin portal to Worker with login and live admin APIs.* |
| Git `main`（admin-portal） | `c974027` — *Add admin login and live API integration for projects, accounts, and conversations.* |
| Cloudflare Worker | `e0bc7a2b`（`jfo-api.jfo-api.workers.dev`） |
| GitHub Pages | 两个仓库推送 `main` 后 Actions 自动构建 |

**涉及文件（主要）**：

- `api-worker/src/admin-portal-auth.ts` — 登录与 Bearer 校验
- `api-worker/src/admin-portal-routes.ts` — bootstrap / 项目 / 用户 / 对话
- `api-worker/src/workspace-user-profiles.ts` — 账号展示元数据
- `api-worker/src/chat-sync.ts` — `loadChatStateData()` 供管理端聚合
- `api-worker/src/index.ts` — 路由与 CORS
- `src/pages/workspace/AdminPortal.tsx` — 跳转独立管理后台

---

## 2026-07-09 · 项目知识网络支持回滚

**摘要**：项目详情抽屉新增「回滚到上一版」，可将当前知识网络恢复为最近归档版本（不新增版本号）。

### 用户可见

- **入口**：项目详情抽屉 → **项目知识网络** 区块。
- **回滚到上一版**（琥珀色按钮）：当存在至少一个归档版本时，具备发布权限的用户可见。点击后确认，即可将当前版撤销并恢复为**最新归档版**（例如 v12 → v11）。
- **恢复此版本**：在版本下拉里选中某一归档版后，可出现「恢复此版本」，效果与回滚相同，但目标为所选归档版。
- **权限**：与「上传 HTML 覆盖」一致——**admin / core**（Worker 侧校验）；访客账号无此操作。
- **确认文案**：会提示「当前版将被撤销；此操作不会新增版本号」。

### 接口 / 后端

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/projects/{projectId}/knowledge-network/rollback?userId=` | 回滚至归档版 |

请求体（可选）：

```json
{ "archiveVersion": 11 }
```

- 省略 `archiveVersion` 时，默认回滚到**最新归档版**（`project_knowledge_network_versions` 中 `version` 最大的一条）。
- 成功响应含 `meta`、`removedVersion`、`removedVersionDisplay`、`message`。

**回滚逻辑（不升版本号）**：

1. 从 R2 读取目标归档 HTML（`v{n}.html`）。
2. 将 `current.html` 写回为该归档内容，并刷新附录 D 版本台账。
3. 更新 D1 `project_knowledge_networks`（`version` / `version_label` / `updated_by` / `changelog` 等）。
4. 从归档表删除已恢复的那条记录；删除被撤销当前版在 R2 上的 `v{bad}.html`（若存在）。

**运维脚本**（与 API 共用同一套逻辑）：

```bash
cd api-worker
npx tsx scripts/rollback-kn-to-archived-version.ts <projectId> <archiveVersion>
```

### 提交 / 部署

| 组件 | 版本 |
|------|------|
| Git `main` | `a8bd7b0` — *Add knowledge network rollback API and drawer UI to restore archived versions.* |
| Cloudflare Worker | `0f011303`（`jfo-api.jfo-api.workers.dev`） |
| GitHub Pages 前端 | 随 `a8bd7b0` 推送后 Actions 自动构建 |

**涉及文件（主要）**：

- `api-worker/src/project-knowledge-network.ts` — `rollbackProjectKnowledgeNetwork()`
- `api-worker/src/project-knowledge-network-routes.ts` — `handleRollbackProjectKnowledgeNetwork`
- `api-worker/src/index.ts` — 路由注册
- `src/lib/project-api.ts` — `rollbackProjectKnowledgeNetwork()`
- `src/components/workspace/ProjectKnowledgeNetworkSection.tsx` — 抽屉按钮与确认流

### 典型场景

**演员 AI 版权投资**（`proj-4a974e67c0f9`）：Jessica 的 v12 修改不符合预期时，管理员在项目抽屉点击「回滚到上一版」，确认后即可回到 v11。

---

<!-- 后续更新请在本分隔线上方追加新条目，保持倒序。 -->

